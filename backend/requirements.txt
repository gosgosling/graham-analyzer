fastapi==0.128.0
uvicorn[standard]==0.40.0
python-dotenv==1.0.1
requests==2.32.5
sqlalchemy==2.0.45
alembic==1.18.1
psycopg2-binary==2.9.11
pydantic-settings==2.12.0
python-multipart==0.0.20

# AI-парсер финансовых отчётов
pymupdf==1.24.13
openai==1.60.0
tenacity==9.0.0

# Тесты расчётного слоя: python -m pytest (из каталога backend)
pytest==9.1.1
# Покрытие — вход для scripts/code_quality.py:
#   pytest --cov=app --cov-report=json:../coverage.json
pytest-cov==7.1.0

# Скрапер e-disclosure (tools/edisclosure-scraper) вызывается из
# app/services/disclosure — значит это зависимость приложения, а не «утилиты».
# Без неё падает скачивание отчётов: ModuleNotFoundError уже после деплоя.
playwright>=1.44.0
beautifulsoup4==4.12.3
lxml==5.3.0
