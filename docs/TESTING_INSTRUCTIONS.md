# 🧪 Инструкция по тестированию Graham Analyzer

## ✅ Что было исправлено

### 1. Список компаний (CompaniesList.tsx)
- ✅ Добавлены раскрывающиеся секции с отчётами под каждой компанией
- ✅ Кликабельные строки компаний для перехода на детальную страницу
- ✅ Модальное окно для просмотра отчётов
- ✅ Кнопки добавления отчётов

### 2. Страница компании (CompanyDetail.tsx)
- ✅ Создан бэкенд эндпоинт `GET /companies/{company_id}`
- ✅ Добавлена функция `get_company_by_id()` в сервис
- ✅ Страница отображает:
  - Основную информацию о компании (FIGI, ISIN, тикер, сектор)
  - Цену акции и капитализацию (из последнего отчёта)
  - Список финансовых отчётов
  
## 🎯 Как протестировать

### 1. Список компаний
1. Откройте http://localhost:5173/companies
2. Вы должны увидеть таблицу с компаниями
3. У каждой компании есть кнопка **▶** в начале строки
4. Нажмите на **▶** - откроется секция с отчётами
5. Если отчётов нет, вы увидите кнопку "Добавить первый отчет"

### 2. Переход на страницу компании
1. Нажмите на **любую строку компании** в таблице
2. Вы перейдёте на `/company/{id}`
3. На странице отобразится:
   - Название компании
   - Тикер и сектор
   - Основная информация (FIGI, ISIN)
   - Список финансовых отчётов (если есть)
   - Цена акции и капитализация (если есть отчёты)

### 3. Проверка бэкенда
```bash
# Получить компанию по ID (например, ID=5)
curl http://localhost:8000/companies/5

# Получить список всех компаний
curl http://localhost:8000/companies/

# Получить отчёты компании
curl http://localhost:8000/reports/company/5
```

## 📝 Что ещё нужно добавить (будущие фичи)

- [ ] График цены акций
- [ ] График мультипликаторов (P/E, P/B и т.д.)
- [ ] Новости компании
- [ ] Подробное описание бизнеса
- [ ] Анализ по методу Грэма
- [ ] Редактирование и удаление отчётов
- [ ] Экспорт данных

## 🚀 Запуск приложения

### Бэкенд
```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --reload
```

### Фронтенд
```bash
cd frontend
npm start
# или PORT=5173 npm start
```

## 📊 Тестовые данные

Для полноценного тестирования добавьте несколько отчётов через админку:
1. Перейдите на страницу компании
2. Раскройте секцию отчётов (кнопка ▶)
3. Нажмите "Добавить первый отчет"
4. Заполните форму с финансовыми показателями
5. Обновите страницу - отчёт должен появиться в списке

## 🐛 Известные проблемы

- Нет валидации на дублирование отчётов (можно добавить два отчёта с одной датой)
- Модальное окно просмотра отчёта показывает не все поля
- Отсутствует пагинация для списка отчётов

---
Последнее обновление: 08.02.2026
