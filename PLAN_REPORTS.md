# План разработки: Ручной ввод финансовых отчетов

## 🎯 Цель
Создать функционал для ручного ввода данных из финансовых отчетов компаний с сохранением в БД.

## 📋 Что нужно сделать

### Этап 1: Backend - Модель данных и БД

#### 1.1 Создать модель FinancialReport
**Файл:** `backend/app/models/financial_report.py`

**Поля модели:**
- `id` - первичный ключ
- `company_id` - внешний ключ к таблице companies (связь с компанией)
- `report_date` - дата отчета
- `price_per_share` - цена акции на дату отчета
- `shares_outstanding` - количество акций в обращении
- `revenue` - выручка
- `net_income` - чистая прибыль
- `total_assets` - общие активы
- `current_assets` - текущие активы
- `total_liabilities` - общие обязательства (долг)
- `current_liabilities` - текущие обязательства
- `equity` - собственный капитал
- `dividends_per_share` - дивиденды на акцию
- `created_at` - дата создания записи
- `updated_at` - дата обновления

**Связь:** ForeignKey к Company

#### 1.2 Создать Pydantic схемы
**Файл:** `backend/app/schemas.py` (добавить)

**Схемы:**
- `FinancialReportCreate` - для создания отчета (входные данные)
- `FinancialReport` - для ответа API (выходные данные)

#### 1.3 Создать миграцию
```bash
cd backend
alembic revision --autogenerate -m "add financial_reports table"
alembic upgrade head
```

---

### Этап 2: Backend - Бизнес-логика

#### 2.1 Создать сервис report_service.py
**Файл:** `backend/app/services/report_service.py`

**Функции:**
- `create_report(db, company_id, report_data)` - создание отчета
- `get_reports_by_company(db, company_id)` - получение всех отчетов компании
- `get_report_by_id(db, report_id)` - получение отчета по ID
- `update_report(db, report_id, report_data)` - обновление отчета
- `delete_report(db, report_id)` - удаление отчета

#### 2.2 Создать роутер reports_router.py
**Файл:** `backend/app/routers/reports_router.py`

**Эндпоинты:**
- `POST /reports/` - создание нового отчета
- `GET /reports/company/{company_id}` - получение всех отчетов компании
- `GET /reports/{report_id}` - получение отчета по ID
- `PUT /reports/{report_id}` - обновление отчета
- `DELETE /reports/{report_id}` - удаление отчета

#### 2.3 Подключить роутер в main.py
Добавить: `app.include_router(reports_router.router)`

---

### Этап 3: Frontend - Типы и API

#### 3.1 Добавить TypeScript типы
**Файл:** `frontend/src/types/index.ts`

```typescript
export interface FinancialReport {
  id?: number;
  company_id: number;
  report_date: string;  // YYYY-MM-DD
  price_per_share: number;
  shares_outstanding: number;
  revenue: number;
  net_income: number;
  total_assets: number;
  current_assets: number;
  total_liabilities: number;
  current_liabilities: number;
  equity: number;
  dividends_per_share: number;
  created_at?: string;
  updated_at?: string;
}

export interface FinancialReportCreate {
  company_id: number;
  report_date: string;
  price_per_share: number;
  shares_outstanding: number;
  revenue: number;
  net_income: number;
  total_assets: number;
  current_assets: number;
  total_liabilities: number;
  current_liabilities: number;
  equity: number;
  dividends_per_share: number;
}
```

#### 3.2 Добавить API функции
**Файл:** `frontend/src/services/api.ts`

```typescript
export const createReport = async (report: FinancialReportCreate): Promise<FinancialReport> => {
  const response = await api.post<FinancialReport>('/reports/', report);
  return response.data;
};

export const getCompanyReports = async (companyId: number): Promise<FinancialReport[]> => {
  const response = await api.get<FinancialReport[]>(`/reports/company/${companyId}`);
  return response.data;
};
```

---

### Этап 4: Frontend - UI компоненты

#### 4.1 Создать компонент ReportForm
**Файл:** `frontend/src/components/ReportForm.tsx`

**Функционал:**
- Форма с полями для ввода всех данных отчета
- Валидация полей (обязательные поля, числовые значения)
- Кнопки "Сохранить" и "Отмена"
- Обработка отправки данных на сервер

**Поля формы:**
1. Дата отчета (date picker)
2. Цена акции (number input)
3. Количество акций (number input)
4. Выручка (number input)
5. Чистая прибыль (number input)
6. Общие активы (number input)
7. Текущие активы (number input)
8. Общие обязательства (number input)
9. Текущие обязательства (number input)
10. Собственный капитал (number input)
11. Дивиденды на акцию (number input)

#### 4.2 Обновить CompaniesList.tsx
**Изменения:**
- Добавить колонку "Действия" в таблицу
- Добавить кнопку "Добавить отчет" в каждой строке
- Добавить модальное окно (Modal) с формой ReportForm
- При клике на кнопку открывать модальное окно с формой
- После успешного сохранения закрывать модальное окно и показывать уведомление

---

## 📊 Структура данных отчета

### Поля для ввода:

| Поле | Тип | Описание | Обязательное |
|------|-----|----------|--------------|
| `report_date` | date | Дата отчета | ✅ |
| `price_per_share` | float | Цена акции на дату отчета | ✅ |
| `shares_outstanding` | integer | Количество акций в обращении | ✅ |
| `revenue` | float | Выручка | ✅ |
| `net_income` | float | Чистая прибыль | ✅ |
| `total_assets` | float | Общие активы | ✅ |
| `current_assets` | float | Текущие активы | ✅ |
| `total_liabilities` | float | Общие обязательства | ✅ |
| `current_liabilities` | float | Текущие обязательства | ✅ |
| `equity` | float | Собственный капитал | ✅ |
| `dividends_per_share` | float | Дивиденды на акцию | ✅ |

### Связь с мультипликаторами (будущий этап):

Из этих данных можно будет рассчитать:
- **P/E** = `price_per_share / (net_income / shares_outstanding)`
- **P/B** = `price_per_share / (equity / shares_outstanding)`
- **Debt/Equity** = `total_liabilities / equity`
- **Current Ratio** = `current_assets / current_liabilities`
- **ROE** = `(net_income / equity) * 100`
- **Dividend Yield** = `(dividends_per_share / price_per_share) * 100`

---

## 🔄 Поток работы

1. **Пользователь открывает список компаний**
   - Видит таблицу с компаниями
   - В каждой строке есть кнопка "Добавить отчет"

2. **Пользователь нажимает "Добавить отчет"**
   - Открывается модальное окно с формой
   - Поле `company_id` автоматически заполняется

3. **Пользователь заполняет форму**
   - Вводит все необходимые данные из финансового отчета
   - Форма валидирует данные

4. **Пользователь нажимает "Сохранить"**
   - Frontend отправляет POST запрос на `/reports/`
   - Backend создает запись в БД
   - Возвращается созданный отчет

5. **Успешное сохранение**
   - Модальное окно закрывается
   - Показывается уведомление об успехе
   - (В будущем) Автоматически рассчитываются мультипликаторы

---

## 📝 Порядок реализации

### Шаг 1: Backend - Модель и БД
1. ✅ Создать `models/financial_report.py`
2. ✅ Добавить схемы в `schemas.py`
3. ✅ Создать миграцию
4. ✅ Применить миграцию

### Шаг 2: Backend - Сервисы и API
1. ✅ Создать `services/report_service.py`
2. ✅ Создать `routers/reports_router.py`
3. ✅ Подключить роутер в `main.py`
4. ✅ Протестировать API через Swagger

### Шаг 3: Frontend - Типы и API
1. ✅ Добавить типы в `types/index.ts`
2. ✅ Добавить API функции в `services/api.ts`

### Шаг 4: Frontend - UI
1. ✅ Создать компонент `ReportForm.tsx`
2. ✅ Обновить `CompaniesList.tsx`
3. ✅ Добавить стили (если нужно)
4. ✅ Протестировать весь функционал

---

## 🧪 Тестирование

### Backend тесты:
- Создание отчета
- Получение отчетов компании
- Валидация данных
- Обработка ошибок

### Frontend тесты:
- Открытие формы
- Валидация полей
- Отправка данных
- Обработка ошибок

---

## 🚀 Следующие этапы (после реализации)

1. **Расчет мультипликаторов** - автоматический расчет при сохранении отчета
2. **Просмотр отчетов** - список всех отчетов компании
3. **Редактирование отчетов** - возможность изменить данные
4. **Удаление отчетов** - удаление ошибочных записей
5. **Валидация данных** - проверка логической корректности (например, equity = assets - liabilities)

---

## 📚 Полезные ссылки

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SQLAlchemy Relationships](https://docs.sqlalchemy.org/en/20/orm/basic_relationships.html)
- [React Modal компоненты](https://ant.design/components/modal/)
- [Ant Design Form](https://ant.design/components/form/)

---

**Дата создания:** 2025  
**Версия:** 1.0
